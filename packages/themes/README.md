# DataZen Theme Packs（已迁移 · archived）

> **2026-08-22 起本目录不再承载主题源码。** 统一插件系统上线后，主题以 extension 的 `contributes.themes` 贡献发布，源码包迁移至 [`packages/extensions/`](../extensions/)：
>
> - `community.slate-blue` → [`packages/extensions/community.slate-blue/`](../extensions/community.slate-blue/)（manifest v2，apiVersion 2）
>
> 旧版 `{appData}/themes/` 运行时导入入口已在主窗口移除（PRD Q8：一次性切换，不做迁移兼容）；本目录保留仅为历史参考。`fixtures/themes/community.fixture-dark` 仍被遗留 theme 模块单测使用，未移动。
