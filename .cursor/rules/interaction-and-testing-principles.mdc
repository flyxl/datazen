---
description: 交互状态机、自动补全与测试设计原则（防过度过滤、防打地鼠式修改、要求连续旅程测试）
globs: src/**/*.{ts,tsx}
alwaysApply: false
---

# 交互与补全开发原则（防回归与测试设计规范）

当你在 `src/` 中开发或修改 UI 交互、CodeMirror 编辑器、自动补全、下拉面板、快捷键或手势逻辑时，**必须严格遵守**以下工程原则：

## 1. 状态机全生命周期（禁止只进不出）
- **核心认知**：用户的输入 90% 以上时间处于“语法残缺、操作进行中”的中间态（In-Flight State）。
- **必须定义退出跃迁**：任何上下文（如 `isTableContext`、`kind === 'table'`）不仅要定义**进入条件**，还必须显式定义**退出/跃迁条件**（如：表名输入完成出现空格、别名或逗号时必须交还控制权，绝不能锁死状态）。

## 2. 软排序优先于硬过滤（Soft Boosting > Hard Filtering）
- **防过度过滤**：不要一遇到干扰项就粗暴 `return null` 或 `filter(item => false)`。
- **权重倾斜**：优先使用 `boost: 10`（提权）和 `boost: -5`（降权）引导补全排序。只有在语法绝对互斥（如 `alias.` 之后绝不可能出现普通关键字）时才允许硬拦截。

## 3. 必须编写「连续旅程测试（Journey Test）」
- **拒绝单点静态切片**：禁止仅测试一条完美完整的静态字符串。
- **全生命周期模拟**：必须编写模拟用户击键全流程的测试（参考 `src/components/sql-editor/completion/__tests__/sqlTypingJourney.test.ts`）：
  - 键入初始字符 -> 验证建议；
  - 键入中间态 -> 验证过渡；
  - 敲击空格/完成当前项 -> 验证旧状态退出；
  - 键入下一个关键字/字段 -> 验证新状态生效且无上一状态残留抑制。

## 4. 警惕环境保真度偏差（JSDOM vs 真机 WebView）
- **不可信 JSDOM 几何坐标**：JSDOM 没有真实的几何排版引擎，`getBoundingClientRect`、`posAtCoords` 在真机 WebView（macOS WKWebView）中点击 Gutter 等非内容区会返回 `null`。
- **数据属性透传**：交互定位一律使用 DOM `dataset` 属性（如 `data-line-from`）直接绑定实体标识，禁止依赖屏幕物理坐标逆向反查。

## 5. 三维影响度自查（3-Way Impact Check）
每次修改代码前自问：
1. **目标达成**：改动是否彻底解决原缺陷？
2. **误伤排查**：是否误杀了看似相似但合法的同类操作？
3. **后置连续**：用户完成这步后紧接着的操作（如敲空格后写 `WHERE`/`JOIN`）是否依然顺畅？
