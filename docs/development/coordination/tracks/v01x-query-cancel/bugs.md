# v01x-query-cancel Bug 清单

| Bug ID | 描述 | 状态 | 重现步骤 | 验证记录 |
|---|---|---|---|---|
| v01x-query-cancel-BUG-001 | **S3：未知 capability 的 Rust cancel IPC fail-open。** `get_capabilities()` 返回 `None` 时，`cancel_query_impl` 跳过 false 检查并继续调用 `driver.cancel_query`；因此未知 capability 在后端仍可能被当作可请求取消，绕过前端 unknown 的禁用门控。正常 QueryPanel 路径不会触发，但旧/直连 IPC 调用仍可触发。 | 已修复 | 1. 注册未提供 capability metadata 的 legacy/test driver（`register_test_driver`）。<br>2. 确认 `get_capabilities(db_type)` 为 `None`。<br>3. 调用 `cancel_query_impl`/`cancel_query`；修复后在调用 driver 前返回 capability unknown 拒绝。<br>4. MockDriver 记录 `cancel_query` 调用次数，修复后保持为 0；显式 supported 路径保持调用。 | `src-tauri/src/commands/query.rs` 现显式区分 `Some(true)`、`Some(false)` 和 `None`；unknown 返回 `UNSUPPORTED_OPERATION:cancel_query:query cancellation capability is unknown`，不会取得 session 或调用 driver。新增 Host Rust 单测验证 legacy MockDriver 未调用；同测保留 supported/unsupported 行为。`CARGO_TARGET_DIR=/private/tmp/datazen-repair-v01x-query-cancel cargo test -p datazen --lib`：1183 passed / 0 failed / 2 ignored。第二轮独立回归：`cargo test -p datazen --lib commands::query::tests::cancel_query` 3 passed / 0 failed；其中 unknown 拒绝且 MockDriver 调用次数为 0。 |
| v01x-query-cancel-BUG-002 | **S3：MySQL/PostgreSQL cancel 作用域过宽。** 旧实现扫描 processlist/`pg_stat_activity` 或按数据库取消，可能中断其他数据库、其他客户端或同一 DataZen 连接的并发查询。 | 已修复（精确 execution handle） | 1. Host 为每次 stream 生成 opaque `executionId`。<br>2. driver 在 prepare 阶段登记 `executionId -> session + 专用 target/control pool`；专用连接分别获取 PostgreSQL backend PID 或 MySQL thread id。<br>3. cancel 只对该 target 执行 `pg_cancel_backend($1)` 或 `KILL QUERY <id>`；pending cancel 通过 `cancel_requested` 延迟到 target ready 前后检查。<br>4. unknown/stale/wrong-session、重复 id、并发 query 隔离均拒绝或独立处理；事务连接明确 unsupported，不回退到宽作用域。 | Host Rust：`1186 passed / 0 failed / 2 ignored`，覆盖 registry 生命周期、wrong-session/stale/并发隔离与事件顺序；driver-api：`99 passed`；MySQL：`72` 个 crate 单元测试及集成测试通过；PostgreSQL：`86` 个 crate 单元测试及集成测试通过。驱动 crate 内断言精确取消 SQL、不扫描 processlist、native capability=true、兼容 wrapper=false；`cargo fmt --all -- --check`、`git diff --check` 通过。 |
