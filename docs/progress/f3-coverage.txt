# F3 coverage (legacy.rs + pairing.rs classify + syncPairing.ts)

## Rust (cargo llvm-cov)

Command:
  cargo llvm-cov -p datazen --lib --json --output-path /tmp/datazen-f3-cov/f3-coverage.json -- data_sync
Filter: filenames containing /data_sync/ AND basename in {legacy.rs, pairing.rs}
Note: a combined cargo test filter with '|' matches no tests (0 run); use substring `data_sync`.
Note: unfiltered `cargo llvm-cov -p datazen --lib` is known to fail on unrelated
      sync::adapter_registry::tests::ensure_type_wire_aliases_succeed.
Note: whole-crate line coverage is NOT the F3 gate.

Threshold: F3 two-file combined line coverage >= 80%.

file             line_cov line_tot    line%  fn_cov  fn_tot      fn%  reg_cov  reg_tot     reg%
legacy.rs              14       14  100.00%       3       3  100.00%       22       22  100.00%
pairing.rs            101      101  100.00%      11      11  100.00%      155      156   99.36%
TOTAL                 115      115  100.00%      14      14  100.00%      177      178   99.44%

Uncovered line numbers (segments with count=0):
  legacy.rs:  (none)
  pairing.rs: [116]  (region only; test-module assert format arm `{msg}`)

F3 Rust two-file combined line coverage 100.00% >= 80% → PASS

## TypeScript (vitest --coverage, include=src/lib/syncPairing.ts only)

Command:
  npx vitest run src/lib/__tests__/syncPairing.test.ts src/locales/locales.test.ts \
    --coverage --coverage.include=src/lib/syncPairing.ts \
    --coverage.reporter=text --coverage.reporter=json-summary --coverage.reporter=text-summary

Tests: 22 passed / 0 failed (8 syncPairing + 14 locales; locales file is coverage-excluded)

file            stmts    branch   funcs    lines   uncovered
syncPairing.ts  79.48%   70.73%   100%     86.11%  34, 70-73, 93, 119
                31/39    29/41    6/6      31/36

Gate metric (same as F1/F2): lines >= 80% → 86.11% PASS

Uncovered line notes:
  34      syncCategory kiwi/superset → 'other' (no kiwi fixture)
  70-73   normalizeSyncFamily sqlserver/mssql + trino/presto aliases
  93      srcCat === 'other' early unsupported (kiwi/superset pair)
  119     non-sql cross-family → unsupported

Note: vitest default `src/lib/**` statements threshold is 80%; this single-file
      run exits 1 on statements 79.48%. Slice gate is **lines**, not that config.
