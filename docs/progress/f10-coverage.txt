# F10 coverage (table_sync.rs)

## Rust (cargo llvm-cov) — table_sync.rs only

Working directory:
  /Users/wuxiaolong/code/rust-projects/datazen/.worktrees/data-sync-navicat

Command:
  cargo llvm-cov -p datazen --lib --json --output-path /tmp/datazen-f10-cov/f10-coverage.json -- \
    commands::sync::tests::sync_table_impl_refuses_overwrite_copy \
    commands::sync::tests::table_sync_module_has_no_drop_insert_body

Filter: filename ending with /commands/sync/table_sync.rs
Note: --ignore-filename-regex negative-lookahead does not isolate files (llvm-cov still totals the whole crate).
Note: other commands/sync files appear in the same JSON but are EXCLUDED from this slice threshold.
Note: cargo tarpaulin is NOT installed; llvm-cov 0.8.7 was used (not BLOCKED).

Threshold: table_sync.rs line coverage >= 80%.

file             line_cov line_tot    line%  fn_cov  fn_tot      fn%  reg_cov  reg_tot     reg%
table_sync.rs          10       28   35.71%       2       4   50.00%        5       14   35.71%
TOTAL                  10       28   35.71%       2       4   50.00%        5       14   35.71%

llvm-cov JSON: /tmp/datazen-f10-cov/f10-coverage.json
cargo-llvm-cov 0.8.7; coverage format version 2.0.1
Tests executed under llvm-cov: 2 passed / 0 failed
  (887 other lib tests filtered out)

Source file size: 32 lines (wc -l). Instrumented unique source lines: 10.

Unique source lines with hasCount segments (F8-style; not the gate metric):
  COVERED  L5, L10, L12, L13
  PARTIAL  L11  (hit + zero region on tracing::info! expansion)
  UNCOVERED L16, L29, L30, L31, L32
  unique-source-line coverage: 5/10 = 50.00%  (still < 80%)

Uncovered line numbers (segments with count=0 and no covered hit on that line):
  table_sync.rs: [16, 29, 30, 31, 32]

Uncovered line notes:
  16                 sync_tables_impl function entry (never called by the two filtered tests)
  29                 resume_offset parameter / signature tail of sync_tables_impl
  30                 tracing::info!(..., "sync_tables refused")
  31                 Err(refuse_overwrite_copy().into())  — second refuse path NOT executed
  32                 function close

Covered line notes:
  5, 10, 11, 12, 13  sync_table_impl (hit by sync_table_impl_refuses_overwrite_copy)
  11                 PARTIAL: tracing::info! has a zero-count expansion region on the same line

Partial (hit + zero region on same line; not counted as uncovered lines):
  11                 tracing::info! macro expansion zero-count region

Functions: llvm-cov reports 4 functions / 2 covered (50%).
  async fn desugars to extra Future/poll items; both uncovered items belong to sync_tables_impl.

Why the specified two tests cannot reach 80%:
  - sync_table_impl_refuses_overwrite_copy executes only sync_table_impl (single-table refuse).
  - table_sync_module_has_no_drop_insert_body is include_str! source assertion; it does not
    execute sync_table_impl or sync_tables_impl.
  - No unit test in commands::sync::tests calls sync_tables_impl.
  - Even unique-source-line metric (5/10 = 50%) is below 80%.

Gate metric (same as F1–F8 Rust): llvm-cov summary.lines >= 80%
  table_sync.rs  35.71% (10/28)  → FAIL

F10 table_sync.rs line coverage 35.71% < 80% → FAIL

## Fallback note (not used)

llvm-cov WAS available, so tarpaulin / BLOCKED fallback was not applied.
For the record: table_sync.rs is 32 lines (~30). The two specified tests cover
ONE runtime refuse (sync_table_impl) + source-string assertion. They do NOT
cover both refuse functions. sync_tables_impl remains unexecuted.
